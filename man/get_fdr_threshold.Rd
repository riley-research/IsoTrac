% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fdr_threshold.R
\name{get_fdr_threshold}
\alias{get_fdr_threshold}
\title{Calculate False Discovery Rate (FDR) Score Threshold}
\usage{
get_fdr_threshold(
  full_mgf,
  full_fdr_mgf,
  aa_seq,
  protein_mass,
  score = c("pearson", "cosine", "NRMSE"),
  number_of_cores = 1,
  isotope_peaks_included = 3,
  cutoff = 0.01,
  ppm_tolerance = 20,
  cosine_score_correction = 1,
  minimal_isotope_sequence = c(-2, -1, 0, 1, 2),
  show_plot = TRUE,
  plot_bins = 0.006
)
}
\arguments{
\item{full_mgf}{\code{data.frame}. The data imported using the \code{\link{import_spectra}}
function.}

\item{full_fdr_mgf}{\code{data.frame}. The processed decoy spectra data frame, typically
generated via \code{generate_fdr_spectra}.}

\item{aa_seq}{\code{character}. The primary amino acid sequence of the target protein backbone.}

\item{protein_mass}{\code{numeric}. Average mass of the bare protein backbone to
deduce potential glycan additions.}

\item{score}{\code{character}. Similarity metric to calculate. Must be one of
\code{"cosine"}, \code{"pearson"}, or \code{"NRMSE"} (Normalized Root Mean Square Error).
Default is \code{"pearson"}.}

\item{number_of_cores}{\code{integer}. Number of parallel workers to allocate for
processing spectra lists via \code{future.apply}. Default is \code{1}.}

\item{isotope_peaks_included}{\code{integer}. Number of isotropic peaks flanking the major
monoisotopic peak to track inside the verification window. Default is \code{3}.}

\item{cutoff}{\code{numeric}. The False Discovery Rate constraint limit (e.g., \code{0.01}
for a 1\\\% FDR cutoff threshold). Default is \code{0.01}.}

\item{ppm_tolerance}{\code{numeric}. Mass accuracy window limit in parts-per-million
applied when matching experimental m/z coordinates to template patterns. Default is \code{20}.}

\item{cosine_score_correction}{\code{numeric}. Intensity multiplier offset constant applied
during manual cosine similarity logic computations. Default is \code{1}.}

\item{minimal_isotope_sequence}{\code{numeric vector}. Continuous isotope index steps relative
to the base profile required to validate and trim isotope fit profiles. Default is \code{c(-2, -1, 0, 1, 2)}.}

\item{show_plot}{\code{logical}. If \code{TRUE}, prints a line plot comparing target and decoy score
binned counts alongside the derived threshold boundary. Default is \code{TRUE}.}

\item{plot_bins}{\code{numeric}. Bin width size used to cluster matching score counts inside the target-decoy visual plot. Default is \code{0.006}.}
}
\value{
\code{numeric}. The calculated score threshold value corresponding to the exact
target FDR allocation parameter restriction.
}
\description{
Evaluates isotope pattern match scores for both target and decoy (FDR) spectra
groups against a predicted molecular formula template. It calculates target and
decoy similarity scores using single-core processing or a parallelized
future backend, estimates q-values using a target-decoy approach,
determines the empirical score cutoff corresponding to a target FDR, and optionally
plots the score distributions.
}
\examples{
\dontrun{
# Standard wrapper calculation at 1\% FDR using 4 parallel processing cores:
fdr_cutoff <- get_fdr_threshold(
  full_mgf = target_spectra,
  full_fdr_mgf = decoy_spectra,
  aa_seq = "PEPTIDEK",
  protein_mass = 910.45,
  score = "pearson",
  number_of_cores = 4,
  cutoff = 0.01
)
}
}
\seealso{
\code{\link{generate_fdr_spectra}}, \code{\link{import_spectra}}
}
