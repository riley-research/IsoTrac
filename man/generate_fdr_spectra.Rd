% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_fdr_spectra.R
\name{generate_fdr_spectra}
\alias{generate_fdr_spectra}
\title{Generate FDR Decoy Spectra Intensities}
\usage{
generate_fdr_spectra(
  full_mgf,
  isotope_peaks_included,
  fdr_approach = "supervised"
)
}
\arguments{
\item{full_mgf}{\code{data.frame}. The data imported using the \code{\link{import_spectra}}
function.}

\item{isotope_peaks_included}{\code{integer}. The number of peaks expected per isotopic cluster
phase (used as the cycle length/bucket count for the simulated intensity trends).}

\item{fdr_approach}{\code{character}. The methodology used for decoy generation. Currently supports
\code{"supervised"} to simulate bounded, cyclic trend intensities. Default is \code{"supervised"}.}
}
\value{
A \code{data.frame} filtered for valid centroid entries, containing either the original
or the newly simulated decoy \code{centroided_intensity} values grouped by spectra ID and slice.
}
\description{
Prepares mass spectrometry data for False Discovery Rate (FDR) estimation by manipulating
peak intensities. When using the default \code{"supervised"} approach, it replaces the original
centroided peak intensities with simulated values that mimic an upward trend over cycles
defined by expected isotopic patterns.
}
\examples{
\dontrun{
# Assuming `processed_mgf` is the output from import_spectra
decoy_mgf <- generate_fdr_spectra(
  full_mgf = processed_mgf,
  isotope_peaks_included = 3,
  fdr_approach = "supervised"
)
}
}
